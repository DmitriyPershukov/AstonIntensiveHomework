spring:
  application:
    name: user-service
  config:
    import: "configserver:http://config-server:${CONFIG_SERVER_PORT}"
  cloud:
    config:
      enabled: true
      fail-fast: true
      retry:
        max-attempts: 3

